document.addEventListener('DOMContentLoaded', function() {
    const headerElement = document.getElementById("header");

    // Make the header visible after the page loads
    headerElement.style.visibility = 'visible';
});
